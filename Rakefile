require "bundler/gem_tasks"

# I don't know how to do this with rspec, but
# you'll want to change this so that if someone types:
#
# rake
#
# It runs the tests by default.
#
# Then you can pass this off to something like TravisCI 
# and it will work.
